/*
 * Future NVS task placeholder.
 * NVS persistence will be added here later; for now no values are loaded,
 * saved, or restored from non-volatile storage.
 */

#include "tasks/nvs.h"

esp_err_t nvs_init(void)
{
    return ESP_OK;
}